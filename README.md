This is the repository for Texera notebook migration tool.

Run ``postgres/add_migration_db.sql`` to add the migration database.

To build the docker image, run ``docker build -t <name> .`` inside the ``migration-microservice`` directory.